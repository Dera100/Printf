#include "main.h"
/**
 * _strlen01 - length of a string in a code
 *
 * @s: the string use to code
 *
 * Return: the length of the str
 */

int _strlen01(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
		;
	return (0);
}
